groups:
  - name: kubecost.rules
    rules:
      - alert: HighDailyCost
        expr: kubecost_cluster_cost_daily > 20
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "High daily cost detected"
          description: "The current estimated daily cost is above $20."
